# Cursor Rules

These extend `core.md` with Cursor-specific guidance. Used in `.cursor/rules` or `.cursorrules`.

## Additional Rules

- Follow the same core coding rules defined in `core.md`.
- Prefer inline suggestions that match the existing code style of the file.
